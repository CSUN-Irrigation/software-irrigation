version: 2.0
jobs:
 Build-Terr:
   docker:
     - image: ubuntu:16.04
   steps:
     - checkout
     - run:
         name: Code Has Arrived
         command: |
           ls -al
           echo '^^^That should look familiar^^^'
     - run:
         name: AWS CLI
         command: |
           apt-get update && apt-get install -y awscli
           aws --version

workflows:
 version: 2
 Example_Workflow:
   jobs:
     - Build-Terr

