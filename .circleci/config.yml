version: 2.0
jobs:
 Build-Terr:
   docker:
     - image: alpine:3.7
   steps:
     - checkout
     - run:
         name: Code Has Arrived
         command: |
           ls -al
           echo '^^^That should look familiar^^^'

workflows:
 version: 2
 Example_Workflow:
   jobs:
     - Build-Terr

